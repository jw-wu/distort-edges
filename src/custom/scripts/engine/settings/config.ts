// Set this to true to see console logs when the functions run.
export const verbose = false;

// Sets the decimal places to round calculation results to, in order to reduce complex numbers.
export const decPlaces = 3;